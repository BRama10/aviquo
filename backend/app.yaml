# app.yaml

runtime: python310
env: standard
entrypoint: gunicorn -b :$PORT core.wsgi:application

handlers:
- url: /static             
  static_dir: static/  
  
- url: /.*
  script: auto


runtime_config:
  python_version: 3
